apiVersion: v1
kind: Service
metadata:
  name: log-triage
  labels:
    app: log-triage
spec:
  type: NodePort
  selector:
    app: log-triage
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
